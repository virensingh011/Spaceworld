// ISS Location
async function getISS() {
  let res = await fetch("https://api.wheretheiss.at/v1/satellites/25544");
  let data = await res.json();
  document.getElementById("iss").innerText =
    `Lat: ${data.iss_position.latitude}, Lon: ${data.iss_position.longitude}`;
}
getISS();

// Space News
async function getNews() {
  let res = await fetch("https://api.spaceflightnewsapi.net/v4/articles/");
  let data = await res.json();

  let list = document.getElementById("news");
  data.results.slice(0,5).forEach(article => {
    let li = document.createElement("li");
    li.innerHTML = `<a href="${article.url}" target="_blank">${article.title}</a>`;
    list.appendChild(li);
  });
}
getNews();

// Weather API (WeatherApi)
async function getWeather() {
  let city = document.getElementById("city").value;
  let apiKey = "3318d63e265b4b46895105610262103";
  let res = await fetch(
    `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${3318d63e265b4b46895105610262103}&units=metric`
  );
  let data = await res.json();

  document.getElementById("weather").innerText =
    `${data.main.temp}°C, ${data.weather[0].description}`;
}

curl -H "Authorization: Bearer <2ffa0b45-bd4c-46b1-8936-14ad9b79ed77>" \
     -H "Content-Type: application/json" \
     -d '{
	"stream": true,
	"model": "DeepSeek-V3.1",
	"messages": [
		{
			"role": "system",
			"content": "You are a helpful assistant"
		},
		{
			"role": "user",
			"content": "Hello!"
		}
	]
	}' \
     -X POST https://api.sambanova.ai/v1/chat/completions
